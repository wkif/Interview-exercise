// 1663. 具有给定数值的最小字符串
function getSmallestString(n: number, k: number): string {
	let ans: string = "";
	return ans;
}

const n = 3,
	k = 27;
const ans = getSmallestString(n, k);
console.log("ans", ans);
